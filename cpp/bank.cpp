#include <bits/stdc++.h>
#define fs first
#define sc second
#define maxn 90005
using namespace std;
int n,a[maxn],m,t[maxn],fsum[maxn];
typedef pair<int,int> II;
II res[maxn];
int main()
{
    freopen("bank.inp","r",stdin);
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin>>n>>m;
    for (int i=1;i<=n;i++)
        cin>>t[i];
    for (int i=1;i<=n;i++)

}
