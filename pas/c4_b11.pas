var
        n:longint;
begin
        write('n la:');
        readln(n);
        if trunc(sqrt(n))=sqrt(n) then write('n la so chinh phuong')
        else write('n ko la so chinh phuong');
        readln
end.