#include<bits/stdc++.h>
using namespace std;
int x,n;
int main()
{
    //freopen("recover.inp","r",stdin);
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin>>n;
    int res=0;
    while(cin>>n)
    {
        for (int i=1;i<=n;i++)
            cin>>a;
        res++;
    }
    cout<<res;
    return 0;
}
